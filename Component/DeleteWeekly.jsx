import { useParams } from "react-router-dom";

let DeleteWeekly= async (id) =>{
    // await axios.delete(`http://localhost:8080/deleteweekly/`);
    // loadUsers();

    // const { id } = useParams();

    return(
        <div>

        </div>
    )
}

export default DeleteWeekly